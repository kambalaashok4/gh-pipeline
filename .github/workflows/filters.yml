name: filters in github action
on:
    push:
        branches: 
            - main
            - feature
    pull_request:
       types:
        - opened
        - closed
       branches:
        - main

jobs:
    hello:
        runs-on: ubuntu-latest
        steps:
        - run: echo worksflow ---- ${{ github.event-name }}


